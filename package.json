{
  "name": "realtime-categories",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -s first \"cd backend && venv\\Scripts\\uvicorn.exe app.main:app --reload --port 8001\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "firebase": "^12.2.1"
  }
}
